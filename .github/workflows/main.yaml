name: mcbayes tests

on:
  push:
    branches:
      - 'main'
  workflow_dispatch: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out BridgeStan
        uses: actions/checkout@v3
        with:
          repository: roualdes/bridgestan
          submodules: recursive
          path: bridgestan

      - name: Set up Julia
        uses: julia-actions/setup-julia@v1
        with:
          version: '1.8'

      - name: Install dependencies
        run: julia -e 'using Pkg; Pkg.develop(PackageSpec(path="bridgestan", subdir="julia")); Pkg.activate("."); Pkg.instantiate(); Pkg.test()'
